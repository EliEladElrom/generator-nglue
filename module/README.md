Modules built for Angular.js in nglue use an interface service in order to expose an api to the apps that use them.

Take an example module, created with command `yo nglue:module map`
a file will be created as modules/map/mapModuleInterface.js

