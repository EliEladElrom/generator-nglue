{
  "name": "<%= _.slugify(projectName) %>",
  "version": "0.0.0",
  "description": "<%= _.slugify(projectName) %>",
  "dependencies": {
  },
  "devDependencies": {
    "grunt": "~0.4.1",
    "grunt-nglue": "latest"
  },
  "engine": "node >= 0.6.6"
}
